# Development Dockerfile for LogDeck Frontend
FROM oven/bun:1-alpine

# Accept build arguments (for future use if needed)
ARG VITE_API_URL

WORKDIR /app

COPY package.json bun.lock* ./

RUN bun install

COPY . .

EXPOSE 5173

# Run Vite dev server with host flag to allow external connections
CMD ["bun", "run", "dev", "--", "--host", "0.0.0.0"]
